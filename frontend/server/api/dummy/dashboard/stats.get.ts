export default defineEventHandler(() => {
  // TODO: replace with a real query once the backend endpoint is ready
  return {
    totalEvents: 12,
    ticketsSold: 3480,
    revenue: 452800000,
    upcomingShows: 3
  }
})